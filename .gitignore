# macOS Finder metadata
.DS_Store

# Third-party source charts used as art references — kept local, never published
source/refs/

# favicon previews from `make-favicon.py --preview`
preview-*.png

# DM-only notes — table secrets that must never reach the public site or its history
bible/dm-only/
